{
  "name": "@ui5/react-playground-shared",
  "version": "0.0.0",
  "devDependencies": {
    "@types/fabric": "^4.2.0",
    "tslib": "^2.0.3",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "dev": "pnpm run dev:watch",
    "dev:watch": "tsc -p . --declaration --watch",
    "build": "pnpm run clean && tsc -p . --declaration",
    "clean": "rm -rf lib",
    "prepare": "pnpm run build"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.5.0",
    "eventemitter3": "^4.0.7",
    "notation": "^2.0.0"
  }
}
